# Python 3.14 (Windows). Rebuild with:
#   py -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# Pinned exactly, on purpose: an embedding model or tokenizer version bump
# silently changes retrieval scores, which would make the ablation table in
# results/ablations.md meaningless. Versions move only as a deliberate commit.

# --- Corpus: scraping + extraction ---
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.3
pymupdf==1.28.2

# --- Embeddings (model: LocalDoc/LocRet-small, see DECISIONS.md D-002) ---
sentence-transformers==6.0.1
torch==2.14.0
transformers==5.16.1

# --- Vector store (see DECISIONS.md D-003) ---
chromadb==1.5.9

# --- Generation. Also used for Ollama, which speaks the OpenAI API format ---
openai==3.10.0

# --- Shared ---
pandas==3.0.5
numpy==2.5.3
python-dotenv==1.2.3

# --- Not yet added ---
# ragas       Phase 3.2 generation metrics
# streamlit   Phase 5.1 interface
