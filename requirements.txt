# Python 3.14 (Windows). Rebuild with:
#   py -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# Pinned exactly, on purpose: an embedding model or tokenizer version bump
# silently changes retrieval scores, which would make the ablation table in
# results/ablations.md meaningless. Versions move only as a deliberate commit.

# --- Phase 1: corpus (scraping + extraction) ---
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.3
pymupdf==1.28.2

# --- Shared ---
pandas==3.0.5
numpy==2.5.3
python-dotenv==1.2.3

# --- Phase 2+ (embeddings, vector store, generation, eval) ---
# Added at the Phase 2 boundary, once the embedding model is chosen in DECISIONS.md.
# Deliberately not installed yet: torch is ~2.5 GB and Phase 0-1 does not need it.
